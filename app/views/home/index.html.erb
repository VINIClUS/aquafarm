<nav class="flex items-center justify-between p-4">
  <a href="/" class="font-semibold">AquaFarm</a>
  <div class="flex items-center gap-3 text-sm">
    <% if user_signed_in? %>
      <%= link_to "Dashboard", root_path, class: ui_link_classes %>
      <%= button_to "Sair", destroy_user_session_path, method: :delete, class: "text-neutral-600 hover:underline" %>
    <% else %>
      <%= link_to "Entrar", new_user_session_path, class: ui_link_classes %>
      <%= link_to "Cadastre-se", new_user_registration_path, class: ui_btn_ghost_classes %>
    <% end %>
  </div>
</nav>
